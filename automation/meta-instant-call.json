{
  "name": "BetterCallz — Meta Lead → Instant AI Call",
  "active": false,
  "nodes": [
    {
      "id": "bettercallz-meta-lead",
      "name": "BetterCallz Meta Lead",
      "type": "n8n-nodes-base.facebookLeadAdsTrigger",
      "typeVersion": 1,
      "position": [
        0,
        0
      ],
      "parameters": {
        "event": "newLead",
        "page": {
          "__rl": true,
          "mode": "id",
          "value": ""
        },
        "form": {
          "__rl": true,
          "mode": "id",
          "value": ""
        },
        "options": {
          "simplifyOutput": false
        }
      }
    },
    {
      "id": "validate-and-normalize",
      "name": "Validate and normalize",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        300,
        0
      ],
      "parameters": {
        "jsCode": "return $input.all().map((item,index)=>{\n const lead=item.json, fields=Object.create(null);\n if(Array.isArray(lead.field_data)) for(const f of lead.field_data) { if(typeof f.name==='string') fields[f.name]=Array.isArray(f.values)?f.values.join(', '):''; }\n else if(lead.data && typeof lead.data==='object') Object.assign(fields,lead.data);\n const str=v=>typeof v==='string'?v.trim():'';\n const id=str(lead.id || lead.meta_lead_id);\n if(!/^[a-zA-Z0-9_-]{1,160}$/.test(id)) throw new Error('Stable Meta lead ID is required; do not use an execution ID');\n let raw=str(fields.phone_number || fields.phone || lead.phone);\n if(!/^[+\\d\\s().-]+$/.test(raw)) throw new Error('Invalid phone format');\n let digits=raw.replace(/\\D/g,'');\n if(digits.length===12 && digits.startsWith('91')) digits=digits.slice(2);\n if(digits.length===11 && digits.startsWith('0')) digits=digits.slice(1);\n if(!/^[6-9]\\d{9}$/.test(digits)) throw new Error('A valid Indian mobile number is required');\n const known=new Set(['phone_number','phone','full_name','first_name','last_name','name','email','company_name','company']);\n const extras=Object.fromEntries(Object.entries(fields).filter(([k])=>!known.has(k)).slice(0,20).map(([k,v])=>[k.slice(0,100),str(v).slice(0,500)]));\n return {json:{source:'meta_lead_campaign',meta_lead_id:id,phone:'+91'+digits,name:str(fields.full_name || fields.name || [fields.first_name,fields.last_name].filter(Boolean).join(' ')).slice(0,200),email:str(fields.email),company:str(fields.company_name || fields.company).slice(0,300),form_id:str(lead.form?.id),page_id:str(lead.page?.id),form_context:str(lead.form?.name).slice(0,2000),additional_fields:extras},pairedItem:{item:index}};\n});"
      }
    },
    {
      "id": "trigger-existing-bettercallz-call",
      "name": "Trigger existing BetterCallz call",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        600,
        0
      ],
      "parameters": {
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "method": "POST",
        "url": "https://demo.bettercallz.com/api/calls",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ $json }}",
        "options": {
          "timeout": 60000
        }
      },
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 3000
    },
    {
      "id": "check-dispatch-result",
      "name": "Check dispatch result",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        900,
        0
      ],
      "parameters": {
        "jsCode": "return $input.all().map(item=>{ if(!item.json.callId) throw new Error('No persisted call ID returned'); if(item.json.dispatchState==='review') throw new Error('Dispatch uncertain. Reconcile this call ID in BetterCallz/Sarvam; NEVER create a replacement lead ID to retry. Call: '+item.json.callId); return item; });"
      }
    },
    {
      "id": "setup-instructions",
      "name": "Setup instructions",
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        0,
        -320
      ],
      "parameters": {
        "content": "## BetterCallz only\nSelect the BetterCallz Page and new form. Use a separate Meta app from Decoory. Configure an HTTP Header Auth credential: Authorization = Bearer <BETTERCALLZ_META_API_TOKEN>. Select it on the HTTP node. Never change the Meta lead ID on retries. This workflow does not write to Sheets or send email.",
        "height": 260,
        "width": 600
      }
    }
  ],
  "connections": {
    "BetterCallz Meta Lead": {
      "main": [
        [
          {
            "node": "Validate and normalize",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Validate and normalize": {
      "main": [
        [
          {
            "node": "Trigger existing BetterCallz call",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Trigger existing BetterCallz call": {
      "main": [
        [
          {
            "node": "Check dispatch result",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1",
    "timezone": "Asia/Kolkata",
    "saveDataSuccessExecution": "none",
    "saveDataErrorExecution": "all"
  }
}
